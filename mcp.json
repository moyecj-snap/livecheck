{
  "mcpServers": {
    "livecheck": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "tsx",
        "${PLUGIN_ROOT}/src/mcp.ts"
      ],
      "env": {
        "LIVECHECK_URL": "https://livecheck.fly.dev/v1/verify"
      },
      "description": "Livecheck MCP: verify, check, confirm, watch (+ verify_listing compat; watch_get/events/stop/chain_topup). No wallet \u2014 unpaid calls return structured 402. Entry: src/mcp.ts \u2192 src/mcp-server.ts."
    }
  }
}
